name: 'Dependency Review'
on:
  pull_request:
    branches: [ "main" ]

permissions: { }

jobs:
  dependency-review:
    uses: coveo/public-actions/.github/workflows/dependency-review-v2.yml@main
    with:
      public: true
    permissions:
      contents: read
      pull-requests: write